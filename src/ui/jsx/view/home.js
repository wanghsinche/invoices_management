import React, { Component, PropTypes} from 'react';
import { Window, Toolbar, Content, Pane } from "../photon/photon";
const Home = ({})=>{
        return (
        <Pane >
                HOME
        </Pane>
        );
};


export default Home;
